# No environment variables are needed for the token sync.
#
# How it works:
#   1. Export your Figma variables as JSON using a plugin
#   2. Save the JSON file(s) into tokens/figma-export/
#   3. Run: npm run sync-tokens
#   4. Run: npm run push-tokens
