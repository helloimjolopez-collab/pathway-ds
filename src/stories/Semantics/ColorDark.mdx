import { Meta } from "@storybook/blocks";
import { createSemanticColors } from "../components/SemanticColors.js";

<Meta title="Tokens/Semantics/Color (Dark Mode)" />

# Semantic Color — Dark Mode

Contextual color tokens for dark mode, grouped by usage: text, fill, icon, stroke, surface, and overlay. Each token references a primitive color.

<div ref={(el) => { if (el && !el.hasChildNodes()) el.appendChild(createSemanticColors("dark-mode")); }} />
