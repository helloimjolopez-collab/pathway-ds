import { Meta } from "@storybook/blocks";
import { createSemanticTypography } from "../components/SemanticTypography.js";

<Meta title="Tokens/Semantics/Typography (Desktop)" />

# Semantic Typography — Desktop

Type scale for desktop breakpoints. Each row shows the token name, size/weight/line-height values, and a live text sample.

<div ref={(el) => { if (el && !el.hasChildNodes()) el.appendChild(createSemanticTypography("desktop")); }} />
