import { Meta } from "@storybook/blocks";
import { createSemanticTypography } from "../components/SemanticTypography.js";

<Meta title="Tokens/Semantics/Typography (Mobile)" />

# Semantic Typography — Mobile

Type scale for mobile breakpoints. Each row shows the token name, size/weight/line-height values, and a live text sample.

<div ref={(el) => { if (el && !el.hasChildNodes()) el.appendChild(createSemanticTypography("mobile")); }} />
