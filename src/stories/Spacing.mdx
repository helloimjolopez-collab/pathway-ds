import { Meta } from "@storybook/blocks";
import { createSpacingScale } from "./components/SpacingScale.js";

<Meta title="Tokens/Spacing" />

# Spacing Tokens

All spacing and unit tokens from the Pathway Design System. Bar width is proportional to the value. Grouped by collection.

<div ref={(el) => { if (el && !el.hasChildNodes()) el.appendChild(createSpacingScale()); }} />
