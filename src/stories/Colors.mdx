import { Meta } from "@storybook/blocks";
import { createColorSwatches } from "./components/ColorSwatches.js";

<Meta title="Tokens/Colors" />

# Color Tokens

All color tokens from the Pathway Design System, grouped by palette. Each swatch shows the token name and hex value.

<div ref={(el) => { if (el && !el.hasChildNodes()) el.appendChild(createColorSwatches()); }} />
